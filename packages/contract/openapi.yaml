components:
  schemas:
    github_com_bingoring_forin_server_internal_domain_auth.TokenPair:
      properties:
        accessToken:
          type: string
        expiresIn:
          description: access token seconds-to-live
          type: integer
        refreshToken:
          type: string
      type: object
    github_com_bingoring_forin_server_internal_domain_progress.Progress:
      properties:
        emergencyResponse:
          type: integer
        level:
          type: integer
        patientSatisfaction:
          type: integer
        peerTrust:
          type: integer
        rank:
          type: string
        streakCurrent:
          type: integer
        streakLongest:
          type: integer
        xp:
          type: integer
      type: object
    github_com_bingoring_forin_server_internal_domain_user.Profile:
      properties:
        destination:
          description: e.g. "us"
          type: string
        enLevel:
          description: e.g. "B1"
          type: string
        job:
          description: 'MVP: "nurse"'
          type: string
        nativeLang:
          description: e.g. "ko"
          type: string
        userId:
          type: string
      type: object
    github_com_bingoring_forin_server_internal_domain_user.User:
      properties:
        createdAt:
          type: string
        id:
          description: UUID
          type: string
        status:
          type: string
      type: object
    internal_adapters_http.attemptReq:
      properties:
        scenarioId:
          type: string
        score:
          type: integer
      type: object
    internal_adapters_http.gradeReq:
      properties:
        grade:
          type: string
      type: object
    internal_adapters_http.loginResp:
      properties:
        tokens:
          $ref: '#/components/schemas/github_com_bingoring_forin_server_internal_domain_auth.TokenPair'
        user:
          $ref: '#/components/schemas/github_com_bingoring_forin_server_internal_domain_user.User'
      type: object
    internal_adapters_http.meResp:
      properties:
        profile:
          $ref: '#/components/schemas/github_com_bingoring_forin_server_internal_domain_user.Profile'
        user:
          $ref: '#/components/schemas/github_com_bingoring_forin_server_internal_domain_user.User'
      type: object
    internal_adapters_http.socialLoginReq:
      properties:
        idToken:
          type: string
        provider:
          type: string
      type: object
  securitySchemes:
    Bearer:
      in: header
      name: Authorization
      type: apiKey
externalDocs:
  description: ""
  url: ""
info:
  description: forin server API — Go stdlib, hexagonal. Contract is Go-first (swag
    → openapi → openapi-typescript).
  title: forin API
  version: 0.1.0
openapi: 3.1.0
paths:
  /attempts:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/internal_adapters_http.attemptReq'
              description: scenario clear
              summary: body
        description: scenario clear
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/github_com_bingoring_forin_server_internal_domain_progress.Progress'
          description: OK
      security:
      - Bearer: []
      summary: Record a cleared scenario (awards XP, advances streak)
      tags:
      - progress
  /auth/logout:
    post:
      responses: null
      security:
      - Bearer: []
      summary: Logout (revoke refresh tokens)
      tags:
      - auth
  /auth/refresh:
    post:
      responses: null
      summary: Rotate refresh token
      tags:
      - auth
  /auth/social:
    post:
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - type: object
              - $ref: '#/components/schemas/internal_adapters_http.socialLoginReq'
                description: provider + ID token
                summary: body
        description: provider + ID token
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/internal_adapters_http.loginResp'
          description: OK
      summary: Social login (Apple/Google/Kakao)
      tags:
      - auth
  /board/today:
    get:
      responses: null
      summary: Today's event board (daily pool)
      tags:
      - content
  /content/manifest:
    get:
      responses: null
      summary: Content manifest (version)
      tags:
      - content
  /departments:
    get:
      responses: null
      summary: List departments (optionally ?profession=nurse)
      tags:
      - content
  /events:
    get:
      responses: null
      summary: List events (optionally ?profession=nurse)
      tags:
      - content
  /interiors/{id}:
    get:
      responses: null
      summary: 'Get a department interior (tile map: regions/rooms/objects/hotspots)'
      tags:
      - content
  /me:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/internal_adapters_http.meResp'
          description: OK
      security:
      - Bearer: []
      summary: Current user + profile
      tags:
      - user
  /me/progress:
    get:
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/github_com_bingoring_forin_server_internal_domain_progress.Progress'
          description: OK
      security:
      - Bearer: []
      summary: Current user's growth
      tags:
      - progress
  /me/review:
    get:
      responses: null
      security:
      - Bearer: []
      summary: Review cards due today
      tags:
      - review
  /me/review/{id}/grade:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/internal_adapters_http.gradeReq'
              description: 'grade: again|hard|good|easy'
              summary: body
        description: 'grade: again|hard|good|easy'
        required: true
      responses: null
      security:
      - Bearer: []
      summary: Grade a review card (SM-2 spaced repetition)
      tags:
      - review
  /scenarios/{id}:
    get:
      responses: null
      summary: Get a scenario (dialogue/quiz/effect steps)
      tags:
      - content
