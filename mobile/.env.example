# forin mobile — public runtime config (EXPO_PUBLIC_* is inlined into the JS bundle).
# Copy to .env and fill in. Social login stays disabled until its client ID is set.

# API base (default http://localhost:8080 if unset)
# EXPO_PUBLIC_API_URL=http://localhost:8080

# Google OIDC — from Google Cloud console (OAuth client IDs).
# The server's GOOGLE_CLIENT_ID must equal whichever ID issues the id_token (iOS aud).
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=

# Kakao OIDC — REST API key from the Kakao developers console.
# Register redirect URI  forin://  and enable OpenID Connect. Server KAKAO_CLIENT_ID = this key.
EXPO_PUBLIC_KAKAO_REST_API_KEY=
